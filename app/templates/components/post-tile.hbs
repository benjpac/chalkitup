<div class="panel panel-success">
    <div class="panel-heading">
        <h3 class="panel-title"><strong>{{post.content}}</strong><small> asked by {{post.author}} on {{moment-format post.created 'L'}}.</small></h3>
    </div>
    <div class="panel-body">
        {{#link-to 'post' post.id}}See all answers{{/link-to}}
    </div>
</div>